# Codex review — 7ac929a

## Commit reviewed

- SHA: `7ac929a` (full SHA in `git log`)
- Subject: `fix(self-check): count real queue statuses, dedupe on pending schedule slot (codex 2×P2, 2a939d6)`
- Reviewed against: `HEAD~1` (`83ebe57`)

## Verdict (verbatim)

> No actionable regressions were found in the changed lines. The status counting now matches the queue's actual terminal/pending statuses, and the self-check dedupe now keys on pending scheduled work rather than creation age.

## Findings (verbatim)

_None. Clean bill._

---

## Session report

Chain of bricks + Codex reviews shipped in this session, in commit order:

| Commit | Type | Subject | Codex outcome |
|---|---|---|---|
| `caf1c93` | feat | council: config membership, grounding contract, health envelope | 2 × P2 (health envelope not in schema; xai not aliased) |
| `df8433c` | fix | council: expose council_health in schema, accept xai alias | clean bill |
| `031b8f1` | fix | gap-detector: advice guard, safe-mode falls through, honour provider disable | 1 × P3 (test-file naming — false positive against repo's `_test_*` convention) |
| `1ac45d2` | fix | gmail: dead tokens raise not vanish, search block names unreadable accounts | 1 × P2 (fetch_per_account_literal swallows GmailApiError) |
| `169e008` | fix | gmail: fetch_per_account_literal surfaces reauth as explicit block, not None | clean bill |
| `f1b2f96` | feat | family facts computed at runtime, scoped DIAG_TOKEN | 1 × P2 (diag scope reached refresh_access_token side-effect path) |
| `5c70ce4` | fix | diag: diag scope gets passive-only /gmail/debug | clean bill on scope split; 1 × P3 (DB conn leak on exception) |
| `6c2ff6a` | fix | diag: close DB connection on passive-path exceptions | clean bill |
| `cae7aa1` | fix | openai: max_completion_tokens for GPT-5.x — seat dark on 400 since model bump | clean bill |
| `2a939d6` | feat | self-check: morning health push — five passive checks | 2 × P2 (wrong done-status name; dedupe race across restarts) |
| **`7ac929a`** | fix | self-check: count real queue statuses, dedupe on pending schedule slot | **clean bill (this review)** |

Verdict artifacts landed for every review at `docs/reviews/<short-sha>.md` per the standing rule established mid-session.

### Session pattern

Each brick followed the same protocol: sandbox-validated patch (or deterministic script when a download failed) → apply → validate (compileall + suite runners, PRoot env failures accepted) → commit → push → Codex review → verdict artifact → push. Every Codex finding raised in this session has now been closed by a same-session follow-up brick.

### Notable behavioural outcomes

- **Council**: `COUNCIL_MEMBERS` env dial honoured with xai→grok alias; `council_health` envelope reaches the API surface via the `CouncilResponse` schema.
- **Gap detector**: advice/planning questions no longer misclassified as capability gaps; safe-mode degrades to answering, not refusing; disabled-provider status short-circuits classifier.
- **Gmail visibility**: dead refresh tokens raise instead of returning `[]`; unreadable accounts named in prompt context on fan-out and per-account literal paths; nothing silently drops.
- **Diagnostics**: `DIAG_TOKEN` scope with passive-only contract on `/gmail/debug` (DB read only, no refresh, no writes, no external calls); source-contract test guards against regressions; connection-lifetime handled through nested try/finally.
- **OpenAI seat**: `max_tokens` → `max_completion_tokens` for GPT-5.x reasoning family (was dark on HTTP 400 since the model bump); regression guarded by source-contract test.
- **Morning self-check**: five passive DB reads (database, gmail, task_queue, errors_24h, council) → one 07:30 push. Task-status names aligned with the real queue vocabulary; dedupe now keys on pending status + scheduled slot rather than 6-hour creation window, so restart-heavy days no longer produce duplicate pushes.

### Open items after this brick

_None. No open Codex findings._
