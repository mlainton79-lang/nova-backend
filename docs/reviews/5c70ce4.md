# Codex review — 5c70ce4

## Commit reviewed

- SHA: `5c70ce47b8d00ca6ce96e4e766a826214e202dee`
- Short SHA: `5c70ce4`
- Subject: `fix(diag): diag scope gets passive-only /gmail/debug, no refresh side effects (codex P2, f1b2f96)`
- Reviewed against: `HEAD~1` (`6045593`)

## Verdict (verbatim)

> The functional change correctly distinguishes dev and diag scopes and keeps the diag branch passive. The only issue found is a low-priority connection cleanup problem on an exceptional diagnostic path, so it does not make the patch incorrect.

## Findings (verbatim)

- **[P3] Close diagnostic DB connections on errors** — `/mnt/sdcard/Download/nova-backend/app/api/v1/endpoints/gmail.py:126-127`
  If this passive diagnostic query raises after `get_conn()` succeeds (for example during `execute()` or `fetchall()`), the broad `except` returns an error response without closing the cursor/connection. This leaks a Postgres connection on a diagnostic path; wrap the cursor/connection in `try/finally` or context managers so failures still close it.
