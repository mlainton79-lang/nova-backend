# codex review — 1e02844

**Commit:** `1e02844 fix(self-check): handler perpetuates the daily chain — heartbeat no longer deploy-dependent`
**Base:** `HEAD~1` (`358f36b`)
**Date:** 2026-07-23

## Verdict

Logically sound. One P2 comment on observability of the new critical failure path. No P0/P1 issues raised.

## Comments

### P2 — Persist failures when extending the self-check chain
**File:** `app/core/self_check.py:211`

> When this new handler call fails to queue the next run, `schedule_todays_self_check()` swallows DB/queue exceptions and returns `None`, so the current task is still marked `done` and the daily heartbeat can stop without a persisted `run_events` error. For this background-task failure path, record the scheduling failure via `record_run_event` so it remains visible after stdout logs roll over.

**Assessment:** valid. The chain is now the primary scheduling mechanism (startup is only bootstrap/backstop); a silent chain-break is the exact class of failure this whole patch was built to prevent. The `print(...)` in the outer `except` isn't durable — Railway stdout rolls over. A follow-up patch should route the exception through `record_run_event` (severity `error`, source `self_check`) so a broken chain shows up in the observability surface even after logs cycle.

Not addressed in this commit — logged for the next self-check patch.

## Sandbox validation

- `python -m compileall -q app` → exit 0
- `python app/core/_test_self_check.py` → 14 tests collected. 3 pass (PassiveContractTests, which read source text only). 11 error with `ModuleNotFoundError: No module named 'psycopg2'` — pure PRoot env gap; every failing test hits the same `import psycopg2` at `self_check.py:21` before any assertion runs. Sandbox with psycopg2 present: 14/14 per the brief.

## Scope

- `app/core/self_check.py`: `schedule_todays_self_check` gained `require_future` boundary; `deliver_self_check` calls it at end of run so every successful (or failed-push) delivery queues tomorrow's task.
- `app/core/_test_self_check.py`: dedupe assertion updated for parametrised predicate (`scheduled_for > %s`). Three new tests cover boundary polarity, chain perpetuation, and chain-scheduling-failure isolation.
