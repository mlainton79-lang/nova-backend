# Codex review — f1b2f96

## Commit reviewed

- SHA: `f1b2f963d1379e4a72b4527cfa74db23bde6a5bc`
- Short SHA: `f1b2f96`
- Subject: `feat(core): family facts computed at runtime, scoped DIAG_TOKEN for read-only diagnostics`
- Reviewed against: `HEAD~1` (`069e193`)

## Verdict (verbatim)

> The patch lowers authentication on `/gmail/debug` while the handler can still refresh and persist Gmail access tokens. This violates the introduced read-only diagnostics-token contract and should be fixed before considering the change correct.

## Findings (verbatim)

- **[P2] Keep the diagnostics token off token-refreshing paths** — `/mnt/sdcard/Download/nova-backend/app/api/v1/endpoints/gmail.py:111-111`
  When `/gmail/debug` is called with the new `DIAG_TOKEN`, it still calls `refresh_access_token(account)`, which can POST to Google's token endpoint and `UPDATE gmail_accounts` with a new access token/expiry whenever a token is near expiry. That makes this endpoint non-read-only despite the scoped-token contract, so a leaked diagnostics token can trigger credential refresh side effects; keep this endpoint behind `verify_token` or use a truly read-only token check path.
