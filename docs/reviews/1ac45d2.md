# Codex review — 1ac45d2

## Commit reviewed

- SHA: `1ac45d2cfec8156d535c6f8b1a286be2ebe93761`
- Short SHA: `1ac45d2`
- Subject: `fix(gmail): dead tokens raise not vanish, search block names unreadable accounts`
- Reviewed against: `HEAD~1` (`031b8f1`)

## Verdict (verbatim)

> The main cross-account paths now surface Gmail failures, but the new raised reauth error is still silently dropped for account-specific literal inbox requests. That leaves a user-visible scenario where the patch does not meet its stated no-silent-drop contract.

## Findings (verbatim)

- **[P2] Surface reauth failures in literal inbox requests** — `/mnt/sdcard/Download/nova-backend/app/core/gmail_service.py:257-257`
  When a user asks for a literal account fetch such as "last 5 emails in <account>" and that account's refresh token is dead, this new raise is caught by `fetch_per_account_literal()`'s broad `except` and converted to `None`, so the chat path falls through to generic Gmail context instead of telling Matthew that the named account needs reauth. Since this change relies on callers surfacing per-account errors, the literal-account caller also needs to return an explicit unreadable-account block rather than swallowing the new `GmailApiError`.
