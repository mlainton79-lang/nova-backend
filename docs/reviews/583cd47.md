# codex review — 583cd47

**Commit:** `583cd47 fix(self-check): record chain-scheduling failures via run_events (codex P2 on 1e02844)`
**Base:** `HEAD~1` (`9fc32fa`)
**Date:** 2026-07-23

## Verdict

Clean pass. No P0/P1/P2/P3 comments.

> The change records the previously silent self-check chain scheduling failure without changing the delivery return contract. I did not find a discrete introduced bug in the diff.

## What was addressed

The prior review's P2 (`1e02844.md`) flagged that the outer `except` around `schedule_todays_self_check(require_future=True)` printed to stdout only, so a broken chain could silently vanish after Railway logs cycled. This commit adds an observability record on that path.

## Scope

- `app/core/self_check.py:212-234` — lazy-imports `record_run_event` + `EventSeverity` inside the chain-failure handler; writes `event_type="self_check_chain_scheduling_failed"`, `severity=ERROR`, `subsystem="self_check.chain"`, with `error_class`, truncated `error_message`, and `metadata={"task_id": task_id}`. Inner `try/except` swallows any observability-side failure so the outer contract "chain failure never breaks delivery" still holds — the observability record is best-effort by design.
- `app/core/_test_self_check.py:187-207` — `test_chain_scheduling_failure_does_not_break_delivery` now also patches `app.observability.record_run_event`, asserts one call, and verifies event_type / subsystem / error_class / error_message fields on the call args.

## Sandbox validation

- `python -m compileall -q app` → exit 0
- `python app/core/_test_self_check.py` → 14 tests collected. 3 pass (PassiveContractTests). 11 error on `ModuleNotFoundError: No module named 'psycopg2'` — same PRoot env gap as prior review; every failure hits `import psycopg2` at `self_check.py:21` before any test logic runs. Sandbox with psycopg2 present: 14/14 per the original brief.
